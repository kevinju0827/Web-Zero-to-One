# M09 Grouping

## The "Why?"

## Goals

Group elements logically to apply targeted styling and prepare for complex layouts.

## Core Concepts

## CheckPoints

* [ ] Wrap major semantic sections of your page (e.g., header, main content area, footer) inside `<div>` containers.
* [ ] Wrap specific highlight words or phrases within your paragraphs using `<span>` tags.
* [ ] Apply unique CSS classes to these newly created `<div>` and `<span>` elements.