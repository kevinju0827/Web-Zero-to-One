# M08 Block and Inline

## Goals

Understand the default display behavior of HTML elements and how they flow on the page.

## CheckPoints

* [ ] Use CSS to change a block-level element's default behavior to `display: inline` or `display: inline-block`.
* [ ] Use CSS to change an inline-level element (like an `<a>` or `<span>`) to `display: block`.